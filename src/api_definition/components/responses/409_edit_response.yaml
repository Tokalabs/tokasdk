409editresponse:
  description: >-
    Resource could not be updated as another resource with the same
    name already exists
  content:
    application/json:
      schema:
        $ref: '../schemas/toka_response/toka_api_response.yaml#/TokaAPIResponse'
    text/html; charset=UTF-8:
      schema:
        type: string